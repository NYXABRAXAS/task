# ProHorizon Scope Tracker

> Enterprise Project & Scope Management System  
> **ProHorizon DigiTech Private Limited**

---

## 🚀 One-Click Render Deployment

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com)

### Steps

1. **Fork / Clone** this repository to your GitHub account
2. Go to [render.com](https://render.com) → **New** → **Blueprint**
3. Connect your GitHub repository
4. Click **Apply** — Render provisions the Web Service + PostgreSQL automatically
5. Wait ~3 minutes for the first build
6. Open the URL Render provides — the app is live ✅

> The `render.yaml` file in this repo configures everything automatically.

---

## 🏗️ Architecture

```
Browser → Render Web Service (Node.js + Express)
                ↓
         Render PostgreSQL (managed database)
```

- **Frontend**: Single-page HTML app served by Node.js (`public/index.html`)
- **Backend**: REST API + Socket.io WebSocket (`/api/v1/*`)
- **Database**: PostgreSQL via Sequelize ORM
- **Auth**: JWT Bearer tokens with refresh token rotation

---

## 📋 Default Login Credentials

> Change these immediately after first login!

| Role | Email | Password |
|------|-------|----------|
| Super Admin | superadmin@prohorizon.com | Admin@1234 |
| Admin | admin@prohorizon.com | Admin@1234 |
| Project Manager | pm@prohorizon.com | Admin@1234 |
| Scope Analyst | analyst@prohorizon.com | Admin@1234 |
| Developer | dev@prohorizon.com | Admin@1234 |
| QA Engineer | qa@prohorizon.com | Admin@1234 |
| Client | client@muthoot.com | Admin@1234 |

---

## 🔧 Local Development

```bash
# 1. Clone repository
git clone https://github.com/your-org/prohorizon-scope-tracker.git
cd prohorizon-scope-tracker

# 2. Install dependencies
npm install

# 3. Configure environment
cp .env.example .env
# Edit .env — set DB_HOST, DB_NAME, DB_USER, DB_PASSWORD

# 4. Start (auto-seeds on first run if DB is empty)
npm start

# 5. Open http://localhost:5000
```

---

## 🌐 API Reference

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/v1/health` | Health check |
| POST | `/api/v1/auth/login` | Authenticate |
| GET | `/api/v1/auth/me` | Current user |
| GET | `/api/v1/projects` | List projects |
| GET | `/api/v1/tasks` | List tasks |
| GET | `/api/v1/scopes` | List scopes |
| GET | `/api/v1/clients` | List clients |
| GET | `/api/v1/users` | List users |
| GET | `/api/v1/reports/dashboard` | Dashboard stats |

Full Swagger docs: `/api/docs` (set `SWAGGER_ENABLED=true` in dev only)

---

## 🔑 Environment Variables

See `.env.example` for the complete list.  
Critical variables on Render (auto-configured via `render.yaml`):

| Variable | Source |
|----------|--------|
| `DATABASE_URL` | Auto-linked from Render PostgreSQL |
| `JWT_SECRET` | Auto-generated by Render |
| `JWT_REFRESH_SECRET` | Auto-generated by Render |
| `PORT` | Injected by Render runtime |

---

## 📁 Project Structure

```
prohorizon-scope-tracker/
├── render.yaml              ← Render deployment blueprint
├── server.js                ← HTTP server + Socket.io + auto-seed
├── package.json
├── .env.example
├── public/
│   └── index.html           ← Frontend SPA (served by Express)
├── src/
│   ├── app.js               ← Express app, middleware, routes
│   ├── config/database.js   ← Sequelize + DATABASE_URL support
│   ├── controllers/         ← Route handlers (13 modules)
│   ├── middleware/          ← Auth, RBAC, audit, rate-limit
│   ├── models/              ← Sequelize models (20 tables)
│   ├── routes/              ← Express routers
│   ├── services/            ← Business logic
│   └── utils/               ← Logger, response helpers
├── database/
│   └── seeders/seed.js      ← Initial data (roles, users, projects)
└── uploads/                 ← File upload storage
```

---

## 📞 Support

**ProHorizon DigiTech Private Limited**  
Email: info@horizonprodata.com  
